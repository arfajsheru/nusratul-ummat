import { Router } from "express";
// import { createDonationController } from "../controllers/donation.controller.js";

const router = Router();

// router.post("/create", createDonationController);

export default router;